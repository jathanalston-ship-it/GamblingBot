"""Core cross-cutting primitives shared by every other package.

Modules: config, logging, clock, types, enums, constants, exceptions.
This package must NOT import from any other momentum.* package (no cycles).
See docs/ARCHITECTURE.md for the full module catalog and diagrams.
"""
