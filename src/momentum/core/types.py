"""Immutable domain value objects (frozen dataclasses).

Bar, Signal, RiskAssessment, Order, Fill, Position, TargetPosition, AccountState.
These are the typed contracts passed between pipeline stages.

Architecture scaffold only — interface contract described below; NO implementation yet.
See docs/ARCHITECTURE.md for the full module catalog and diagrams.
"""
